Files for Assignment 2:
- `planner_launch.py`: The launch file that launches **map_server** (used for showing the map in rviz) and the **grading scout**.
- `map.yaml`: map used in **map_server** node
- `cave_filled.png`: Bitmap depicting the map. The bitmap is used in `map.yaml`
- `planning.rviz`: An example Rviz config
